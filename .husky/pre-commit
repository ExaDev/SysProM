staged=$(git diff --cached --name-only --diff-filter=ACMR -- '*.ts' | tr '\n' ' ')
if [ -n "$staged" ]; then
  npx eslint --fix --no-error-on-unmatched-pattern $staged || true
  echo "$staged" | xargs git add
fi

pnpm _typecheck
